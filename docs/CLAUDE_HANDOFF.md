# Claude Handoff

This file is intentionally short now. The current, detailed handoff for Claude or read-only agents lives in:

- `docs/AGENT_HANDOFF.md`

Canonical Codex records:

- `docs/Codexlog.md`: chronological work log.
- `docs/Codexplan.md`: current execution plan.
- `docs/Codex观察记录.md`: reference app interaction observations.
- `docs/Codexplan.md`: reuse boundary and license/code-copying rules.

Do not rely on older handoff text that described the branch as clean or the app as a 3-tab app; the current app is Android-first and uses `待办 / 计时 / 统计 / 我的`.
