# Workspace Naming

- Clean/public workspace name: `Focus_Timer`
- Legacy working workspace name: `focus_timer`
- Keep them separate; do not rely on case-only differences inside the same Windows folder.

This note exists so future work does not drift back to the old naming.
